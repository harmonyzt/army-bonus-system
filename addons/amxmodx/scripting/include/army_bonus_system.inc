native get_user_exp(id);

native get_user_expto(id);

native get_user_bonus(id);

native get_user_rankname(id);

native get_user_lvl(id);

native set_user_bonus(id,num);

native set_user_exp(id,num);

native set_user_lvl(id,num);